# Companion 触发矩阵

## 说明

本表用于梳理 `DyberPet` 离线陪伴功能当前的触发来源、触发条件、冷却策略和输出类型。

目标：

- 统一后续开发认知
- 降低重复触发和逻辑散落
- 便于后续调参与测试

---

## 触发矩阵

| 触发类型 | 触发来源 | 是否主动 | 触发条件 | 冷却 | 计入每日上限 | 专注中可触发 | 当前输出 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `companion_greeting` | 启动问候流程 | 否 | 程序启动或切换宠物后的问候阶段 | 无单独冷却 | 否 | 是 | 陪伴气泡 |
| `companion_patpat` | `patpat()` | 否 | 用户摸头，且满足该类型冷却 | 20 秒基础冷却，受频率倍率影响 | 否 | 是 | 陪伴气泡 |
| `companion_return` | `patpat()` | 否 | 用户再次互动，且距离上次互动超过 30 分钟 | 20 秒基础冷却，受频率倍率影响 | 否 | 是 | 陪伴气泡 |
| `companion_focus_start` | `_change_time('focus_start')` | 否 | 专注开始 | 15 秒基础冷却，受频率倍率影响 | 否 | 是 | 陪伴气泡 |
| `companion_focus_end` | `_change_time('focus_end')` | 否 | 专注结束 | 15 秒基础冷却，受频率倍率影响 | 否 | 是 | 陪伴气泡 |
| `companion_late_night` | 定时轮询 | 是 | 当前处于深夜时段，开启夜间陪伴，总间隔和单类冷却满足 | 1 小时基础冷却，受频率倍率影响 | 是 | 否 | 陪伴气泡 |
| `companion_working` | 定时轮询 | 是 | 前台应用分类为 `ide/design/office`，开启场景陪伴，总间隔和单类冷却满足 | 30 分钟基础冷却，受频率倍率影响 | 是 | 否 | 陪伴气泡 |
| `companion_idle_presence` | 定时轮询 | 是 | 距离上次用户互动超过 45 分钟，总间隔和单类冷却满足 | 1 小时基础冷却，受频率倍率影响 | 是 | 否 | 陪伴气泡 |

---

## 总体约束

### 1. 总间隔约束

主动陪伴除了单类冷却，还要满足总间隔约束。

当前逻辑：

- `low`：约 20 分钟总间隔
- `medium`：约 15 分钟总间隔
- `high`：约 10 分钟总间隔

说明：

- 即使切到 `high`，也仍然不是高频刷屏模式。

### 2. 每日主动上限

当前逻辑：

- `low`：6 次
- `medium`：10 次
- `high`：14 次

计入上限的类型仅包括主动陪伴：

- `companion_late_night`
- `companion_working`
- `companion_idle_presence`

不计入上限的类型：

- 启动问候
- 摸头反馈
- 返回反馈
- 专注开始 / 结束

### 3. 频率倍率

当前频率通过倍率影响冷却：

- `low`：1.0
- `medium`：0.75
- `high`：0.5

---

## 应用场景分类

### 当前会触发工作陪伴的分类

- `ide`
- `design`
- `office`

### 当前不会触发工作陪伴的分类

- `browser`
- `explorer`
- `general`

### 黑名单

当前支持 `_app_blacklist`，命中后统一视为 `general`。

当前默认黑名单包括：

- `wechat`
- `qq`
- `discord`
- `telegram`
- `steam`
- `bilibili`
- `douyin`

---

## 已知边界

### 1. 返回触发目前挂在 `patpat`

当前“用户离开后返回”的判断不是由系统前后台事件直接触发，而是由用户下一次主动互动时触发。

这意味着：

- 返回感知是“互动驱动”的
- 还不是严格意义上的系统回归检测

### 2. 主动轮询依赖定时器

当前主动陪伴通过定时器轮询驱动，属于简单稳定实现。

优点：

- 结构简单
- 风险低

限制：

- 时机精确度一般
- 仍需观察实际体验是否足够克制

### 3. 旧气泡与新陪伴气泡存在体验重叠

当前以下场景仍可能存在边界重叠：

- 启动问候
- 摸头反馈
- 专注提示

后续需要进一步统一旧气泡与陪伴气泡的分工。

---

## 后续建议

下一步建议重点观察：

1. 主动轮询在真实使用中是否仍偏频繁
2. `high` 频率是否依然足够克制
3. 深夜陪伴是否过于敏感
4. 工作陪伴是否有误判场景
