# DyberPet 离线轻互动陪伴功能开发路径设计（项目适配版）

## 1. 文档目的

这份文档用于替换原来的通用型方案，目标不是重新设计一个“AI 陪伴产品”，而是基于当前 `DyberPet` 代码与桌宠形态，规划一条更适合本项目的单机、离线、轻互动陪伴功能开发路径。

这版文档重点解决三个问题：

1. 原方案有不少“从零搭架构”的内容，与当前项目实际技术栈不符。
2. 当前仓库其实已经有一部分离线陪伴能力，原方案没有把这些已实现内容纳入规划。
3. 这次目标明确是“单机、离线、轻互动陪伴”，因此不应把云端 LLM、复杂记忆系统、多模态感知放在主线路线上。

---

## 2. 当前项目现状判断

从仓库现状看，`DyberPet` 已经不是纯桌宠骨架，而是一个已经具备以下基础能力的桌面宠物项目：

- 基于 `PySide6` + `qfluentwidgets` 的桌面应用，而不是 Electron / Tauri。
- 已有完整的宠物动画、拖拽、掉落、随机动作、状态栏、背包、任务、番茄钟等系统。
- 已有气泡系统 `BubbleManager`，能承接“陪伴文本”这类轻互动输出。
- 已有前台窗口识别 `ActiveWindowTracker`，能识别 Windows / macOS 的前台应用窗口。
- 已有离线陪伴原型 `OfflineCompanion`，包含：
  - 回归问候
  - 摸头反馈
  - 专注开始 / 结束提示
  - 深夜低频陪伴
  - 工作场景陪伴
  - 长时间无互动时的静默陪伴
- 设置面板中已经有“Offline Companion”相关开关与频率设置。

结论：

```text
这个项目当前不是“从 0 开始做离线陪伴”，
而是“已有第一版离线陪伴雏形，需要整理成正式能力并继续扩展”。
```

---

## 3. 原文档的主要问题

原文档有价值，尤其是“陪伴优先、低打扰、生命感优先于智能感”这些产品原则是对的。但如果直接照着做，会在项目层面出现偏差。

### 3.1 技术栈不匹配

原文档推荐了：

- Electron / Tauri
- Live2D / Spine
- Rust / C++
- SQLite + 向量库
- WebSocket AI 通信

这些都不适合作为当前项目的主线方向。当前项目已经稳定运行在：

- Python
- PySide6
- 本地 JSON / 现有配置结构
- 现有动画与行为线程模型

如果现在为了“陪伴功能”去切 Electron/Tauri 或引入新的客户端架构，成本非常高，而且会破坏已有桌宠工程积累。

### 3.2 方案默认“云端 AI 是后面主轴”，不适合本次目标

本次你明确要做的是：

```text
单机、离线、轻互动陪伴式功能
```

这意味着：

- 云端 LLM 不是主路径
- 复杂长记忆不是主路径
- AI 智能问答不是主路径
- 麦克风 / 摄像头 / OCR 不是主路径

原文档虽然提到“本地优先”，但整体仍然是一个“未来会走向 AI 陪伴系统”的大方案。对现在这个阶段来说，太重。

### 3.3 没有贴合 DyberPet 现有模块边界

当前项目已经有这些实际接入点：

- `DyberPet.py` 主宠物行为与 UI
- `bubbleManager.py` 气泡输出
- `active_window.py` 前台窗口识别
- `settings.py` 离线陪伴开关和参数
- 专注 / 番茄钟 / 摸头等事件入口

原文档没有围绕这些真实模块设计增量开发路径，因此不利于直接落地。

### 3.4 “记忆系统”“人格系统”写得过重

对桌宠来说，人格当然重要，但当前阶段不需要引入完整的“人格引擎”“记忆分层系统”。

更适合这个项目的方式是：

- 先用固定语气库 + 场景规则，做出稳定人格感
- 再做轻量状态变量
- 最后再决定是否需要更正式的记忆层

换句话说，现阶段应该优先做：

```text
稳定、自然、低打扰、可配置
```

而不是：

```text
复杂、宏大、像通用 AI 产品架构图
```

---

## 4. 本项目的正确定位

### 4.1 产品定位

DyberPet 的离线轻互动陪伴功能，建议定位为：

```text
一个长期驻留桌面的、低打扰的、基于场景和行为规则的陪伴式桌宠系统
```

它不是：

- 聊天机器人
- AI 助手
- 办公提醒器
- 情绪分析器

它更像：

- 会陪着你工作的桌宠
- 会在合适的时候轻轻冒一句话的陪伴角色
- 通过行为、停留、低频互动建立存在感的数字伙伴

### 4.2 当前阶段目标

本轮开发目标建议明确为：

```text
在不引入在线依赖的前提下，
把现有桌宠升级为“更像在陪着你”的轻互动陪伴体。
```

成功标准不是“能聊很多”，而是：

- 它更自然
- 它更贴场景
- 它不烦人
- 它有稳定角色感
- 它能长期开着

---

## 5. 设计原则

### 5.1 陪伴优先，不做问答优先

优先级应该是：

```text
存在感 > 陪伴感 > 自然反馈 > 可持续使用 > 智能感
```

不要把主功能做成“用户说一句，宠物答一句”的聊天产品。

### 5.2 低打扰是最高约束

桌宠和聊天框最大区别在于它会长期停留在桌面上，因此比“会说话”更重要的是“知道什么时候少说话”。

必须控制：

- 触发频率
- 重复度
- 深夜打扰
- 工作中断感
- 同一场景反复冒泡

### 5.3 本地规则优先，避免过早引入 AI

在本项目里，大量陪伴效果其实可以通过以下方式达成：

- 时间段规则
- 前台应用分类
- 用户交互事件
- 低频随机权重
- 短句文案库

这些机制比云端 AI 更稳定、更便宜、更可控，也更符合单机离线目标。

### 5.4 输出短句化，不做长文本

桌宠气泡更适合：

- 4 到 18 字短句
- 轻语气
- 观察式表达
- 陪伴式表达

不适合：

- 长段建议
- 说教
- 任务安排
- 大段情感分析

### 5.5 先统一“角色气质”，再扩展功能

当前阶段不建议一开始就支持很多人格模板。

建议先固定一套默认人格：

```text
安静、温和、轻微观察感、少建议、多陪伴
```

先把这一套体验打磨顺，再考虑扩展风格。

---

## 6. 适合 DyberPet 的能力分层

结合当前项目，离线陪伴功能建议拆成四层，而不是原文档里的大而全架构。

### 6.1 输入层：轻量场景感知

只保留本项目已经能拿到、且隐私成本低的数据：

- 当前时间
- 前台应用 / 前台窗口
- 是否处于专注计时
- 是否刚刚被点击 / 摸头
- 用户是否离开后返回
- 今日触发次数
- 当前宠物基础状态

明确不做：

- OCR
- 麦克风监听
- 摄像头
- 文件内容读取
- 剪贴板分析

### 6.2 判断层：离线陪伴规则引擎

核心职责：

- 什么时候可以说
- 现在该说哪一类话
- 这句话会不会太打扰
- 今天是否已经说太多

这层本质上就是把 `OfflineCompanion` 从“文本生成器”升级成“陪伴调度器”。

### 6.3 输出层：气泡 / 动作 / 小反馈

输出不应只有文本，应该组合：

- 对话气泡
- 对应 icon
- 简单动作切换
- 与场景配套的轻表情反馈

长期看，“陪伴感”很多不是来自文本本身，而是来自：

- 出现时机对
- 句子短
- 动作刚好

### 6.4 配置层：用户可关闭、可调频、可理解

离线陪伴功能必须可控：

- 总开关
- 主动气泡开关
- 工作场景开关
- 深夜提醒开关
- 频率设置

并建议后续补充：

- 场景白名单 / 黑名单
- 静默时段
- 工作模式更克制

---

## 7. 当前代码基础与建议调整

这一部分是最重要的“项目适配”内容。

### 7.1 现有基础可以直接复用

当前这些实现建议保留并继续扩展：

#### A. `OfflineCompanion`

已经具备：

- 冷却时间
- 每日触发上限
- 深夜判断
- 前台应用分类
- 摸头 / 返回 / 专注 / 工作 / 空闲等触发类型

这已经是很好的第一版核心，不建议推翻重写。

#### B. `ActiveWindowTracker`

已经能在 Windows / macOS 上读取前台窗口和进程名，这足够支撑第一阶段的场景陪伴。

#### C. `BubbleManager`

现有气泡系统已经成熟，应该继续作为统一出口，避免另起一套“陪伴消息 UI”。

#### D. `settings.py` + `BasicSettingUI.py`

离线陪伴相关设置项已经落地，这说明产品形态已经开始形成，文档和后续代码应当围绕这套设置体系继续发展。

### 7.2 当前存在的结构问题

虽然基础不错，但当前这套离线陪伴实现还比较“像功能补丁”，没有完全成为独立能力。

建议调整点如下。

#### A. 陪伴逻辑与气泡逻辑边界还不够清晰

当前项目里：

- 原始气泡系统负责基础泡泡类型
- `OfflineCompanion` 负责新陪伴泡泡类型

建议后续明确：

- `BubbleManager` 负责“显示与基础气泡协议”
- `OfflineCompanion` 负责“离线陪伴规则决策”
- 主窗口只负责“事件接入与结果派发”

这样后续不会把各种规则继续散落到 UI 主类里。

#### B. 场景分类还偏硬编码

当前 `APP_CATEGORY_PATTERNS` 直接写在代码里，适合 MVP，但不适合后面扩展。

建议后续改成配置化：

- 默认内置分类
- 允许 JSON 配置扩展关键词
- 支持用户忽略某些应用

#### C. 人格一致性仍然主要靠文案人工维持

这不是坏事，但要进一步规范：

- 文案长度范围
- 语气规则
- 禁止项
- 场景到语气的映射

建议新增一份“陪伴文案规范”，避免以后文本风格越来越散。

#### D. 触发来源还不够统一

后续需要把以下事件统一成“陪伴事件总线”或统一接口：

- 启动问候
- 摸头
- 返回
- 专注开始 / 结束
- 深夜
- 工作中
- 长时间静置

不一定要做复杂事件系统，但至少要形成统一的调用协议，而不是每个入口各自拼逻辑。

### 7.3 当前阶段不建议做的调整

以下方向暂时不要进入主线：

- 引入 SQLite 只为陪伴功能存储
- 引入向量数据库
- 为离线陪伴单独重写调度框架
- 引入复杂情绪模型
- 引入大段“关系成长系统”
- 过早做本地 LLM 集成

这些会显著拉高复杂度，但不会明显提升第一阶段体验。

---

## 8. 新版开发路线

下面这版路线只围绕“单机、离线、轻互动陪伴”展开。

---

## Phase 1：把现有离线陪伴原型做成正式能力

### 目标

```text
先把“已经存在的离线陪伴功能”打磨成稳定、自然、可交付的第一版。
```

### 已有基础

- `OfflineCompanion`
- 前台窗口识别
- Companion 设置项
- companion 文案配置

### 本阶段要做的事

#### 1. 梳理陪伴触发入口

明确当前有哪些事件会触发陪伴：

- 启动时问候
- 摸头时反馈
- 用户长时间离开后返回
- 开始专注
- 结束专注
- 深夜低频提示
- 工作应用场景低频陪伴
- 长时间静置陪伴

把这些入口统一登记到一张“事件-触发矩阵”里。

#### 2. 统一陪伴消息生命周期

每类陪伴消息需要明确：

- 触发条件
- 冷却时间
- 是否计入当日主动次数
- 是否可在专注模式下触发
- 是否可与普通气泡并存

#### 3. 打磨文案库

当前文案已经有第一版，但还需要继续规范：

- 更短
- 更自然
- 更少“功能提示感”
- 更少重复句式

建议建立规则：

- 单句优先
- 少用命令语气
- 少用解释语气
- 少用“建议你”
- 多用“我在”“慢慢来”“一起待着”

#### 4. 补足可视化开关逻辑

确保设置中的以下选项行为真实可控：

- 总开关
- 主动陪伴
- 工作场景陪伴
- 深夜陪伴
- 互动频率

并补充“关闭后是否立即生效”的验证。

### 验收标准

- 连续使用 1 小时，气泡频率自然，不会明显烦人。
- 开启 / 关闭配置项后，行为能明显对应变化。
- 摸头、专注、返回、深夜等典型场景都有稳定反馈。
- 不依赖网络即可完整运行。

---

## Phase 2：补齐“轻场景感”和“存在感”

### 目标

```text
让桌宠不只是“会冒泡”，而是“会在合适的场景出现合适的陪伴”。
```

### 本阶段重点

#### 1. 扩展前台应用场景分类

目前只有粗粒度类别：

- ide
- design
- office
- browser
- explorer

建议扩展方式：

- 保持大类不变
- 增加更多常见应用关键词
- 增加“未知应用不触发主动陪伴”的策略
- 增加应用黑名单

优先保证“宁可少触发，也不要误判乱说”。

#### 2. 增加静默时段与低打扰模式

建议新增：

- 静默时段
- 深夜更低频
- 专注模式完全静默或只保留必要陪伴
- 高频工作场景下的额外冷却

#### 3. 把“动作反馈”纳入陪伴系统

当前陪伴更多体现在文本上，后续建议逐步接入动作：

- 某些气泡触发时切换特定轻动作
- 深夜时更偏安静动作
- 工作中更偏驻留 / 观察动作
- 摸头反馈与 `patpat` 更好联动

注意：这里只做轻联动，不重做动画系统。

#### 4. 增加“无文字陪伴”比例

这是非常适合桌宠的一点。

不是所有陪伴都要说话，可以加入：

- 更安静的停留
- 微小动作变化
- 少量表情 / icon 变化

目标是降低“每次存在感都要靠文本”的依赖。

### 验收标准

- 用户能明显感到桌宠“更会看场景”。
- 工作时更克制，休息时更柔和。
- 深夜不会频繁冒泡。
- 同样一小时使用中，存在感更强但打扰感不增加。

---

## Phase 3：建立轻量“角色一致性”

### 目标

```text
让它不只是规则触发器，而是一个气质稳定的陪伴角色。
```

### 本阶段不做复杂人格引擎

不建议引入：

- MBTI 式人格系统
- 大型状态机人格图
- 多角色动态切换

建议做轻量角色约束。

### 本阶段要做的事

#### 1. 定义角色说话规范

建议单独文档化：

- 常用句式
- 禁止句式
- 语气边界
- 对不同场景的措辞偏好

例如：

- 不说“你应该”
- 不说“建议你立刻”
- 不做家长式提醒
- 不做效率导师
- 不做过度热情的拟人撒娇

#### 2. 建立轻量内部状态变量

可以考虑保留很少几个状态倾向值，用来改变触发概率而不是直接生成复杂内容：

- calmness
- sociability
- energy

这些值只影响：

- 是否更容易主动冒泡
- 夜间是否更安静
- 是否偏静态动作

而不需要发展成完整“情绪引擎”。

#### 3. 做角色风格包的最小抽象

如果后续想扩展不同陪伴风格，建议只抽象到：

- 文案库
- icon 映射
- 场景偏好参数

而不是抽象整个系统。

### 验收标准

- 文案风格明显统一。
- 不同场景下说话仍像同一个角色。
- 角色感主要靠“稳定气质”而不是“花哨文本”。

---

## Phase 4：轻量陪伴成长，但仍保持离线

### 目标

```text
在不做复杂记忆系统的前提下，增强“长期陪着你”的感觉。
```

### 建议方向

#### 1. 共处统计

利用当前已有系统，补一些很轻的长期数据：

- 陪伴天数
- 专注时长累计
- 摸头次数
- 在不同场景中陪伴时长

这类信息可以用于非常低频的特殊气泡。

#### 2. 里程碑型互动

例如：

- “已经一起待了 7 天”
- “今天陪你认真很久了”
- “最近常常见你在画图 / 写代码”

注意要非常低频，否则会显得生硬。

#### 3. 轻量偏好记忆

如果后续确实要做“记住一点点用户”的能力，建议先只做本地、显式、可删除的轻量偏好：

- 用户称呼
- 是否喜欢深夜陪伴
- 哪类场景少打扰

不要直接做自动长期记忆。

### 验收标准

- 用户能感觉到“它陪了我一段时间”。
- 这些长期表达不会显得用力过猛。
- 所有长期数据都可本地查看、可关闭、可清理。

---

## 9. 功能优先级建议

### P0：必须优先做

- 把现有 `OfflineCompanion` 接入路径梳理完整
- 梳理所有触发条件与冷却策略
- 打磨文案库
- 验证所有 Companion 设置项生效
- 保证离线模式完整可用

### P1：应尽快补齐

- 应用分类扩展
- 黑名单 / 静默策略
- 与动作系统的轻联动
- 更自然的返回 / 空闲 / 工作中陪伴

### P2：可以后做

- 轻量状态变量
- 长期共处统计
- 里程碑型陪伴文案
- 可扩展角色风格包

### P3：本轮不建议进入主线

- 云端 LLM 对话
- 本地大模型对话
- 长记忆系统
- 情绪分析系统
- OCR / 麦克风 / 摄像头
- 向量数据库

---

## 10. 推荐的数据与配置策略

为了贴合当前项目，建议继续以 JSON 配置为主，不要因为“陪伴功能”单独引入重量级存储。

### 建议保留 JSON 的内容

- 文案配置
- 场景分类关键词
- 冷却参数
- 频率参数
- 角色风格参数

### 后续可以新增的配置文件

#### `res/icons/companion_conf.json`

继续作为文案主配置文件，但建议未来支持：

- 分类注释说明
- 去重检查
- 场景标签更清晰

#### `res/icons/companion_rules.json`

可选新增，用于存放：

- 默认冷却时间
- 每日上限
- 深夜时间段
- 频率倍率

这样 `OfflineCompanion` 里的硬编码会更少。

#### `data/companion_state.json`

如后续需要长期轻量状态，可考虑单独保存：

- 当日触发计数
- 上次互动时间
- 陪伴累计统计

但这一步不急，能不加就先不加。

---

## 11. 实现建议：按当前代码结构落地

### 建议的模块职责

#### `DyberPet/companion.py`

职责：

- 离线陪伴规则判断
- 冷却控制
- 场景分类
- 文案选择

后续应继续作为核心模块。

#### `DyberPet/bubbleManager.py`

职责：

- 气泡结构协议
- 泡泡显示入口
- 普通泡泡与陪伴泡泡统一出口

避免在多个地方直接拼 bubble dict。

#### `DyberPet/active_window.py`

职责：

- 前台窗口感知
- 返回应用名 / 类名 / 窗口范围

保持轻量，不把陪伴规则塞进去。

#### `DyberPet/DyberPet.py`

职责：

- 接事件
- 调 `OfflineCompanion`
- 把结果发送给气泡系统和动作系统

不要让它继续膨胀成“又做 UI、又做规则、又做文案”。

#### `DyberPet/DyberSettings/BasicSettingUI.py`

职责：

- 配置开关
- 频率控制
- 后续静默策略等设置入口

---

## 12. 开发顺序建议

建议你按下面顺序推进，而不是同时铺很多线：

### 第一步

先做“现有离线陪伴能力盘点与整理”：

- 哪些已经生效
- 哪些还没接到主流程
- 哪些配置项只是 UI 有、逻辑还不完整

### 第二步

把 `OfflineCompanion` 作为正式模块打磨：

- 整理触发矩阵
- 补测试场景
- 统一冷却策略
- 优化文案库

### 第三步

扩展场景陪伴：

- 前台应用分类增强
- 黑名单
- 静默时段
- 工作模式更克制

### 第四步

再做动作联动和轻量成长：

- 场景对应动作
- 低频里程碑气泡
- 轻量长期统计

---

## 13. 最终建议结论

这次方向建议收敛成一句话：

```text
不要把 DyberPet 的离线陪伴功能做成“小型 AI 助手”，
而要把它做成“更自然、更克制、更像在陪着你的桌宠”。
```

真正适合这个项目的路线不是：

- 新技术栈
- 大模型优先
- 复杂情绪 / 记忆架构

而是：

- 复用现有 PySide6 桌宠架构
- 扩展 `OfflineCompanion`
- 继续围绕气泡、动作、场景、低频互动打磨
- 坚持单机、离线、轻互动、低打扰

如果按这个方向推进，DyberPet 的陪伴感会更统一，也更符合桌宠这个品类本身的优势。

---

## 14. 下一步可直接开工的任务清单

建议下一轮直接做下面 5 项：

1. 盘点 `OfflineCompanion` 当前实际接入点，列出现状表。
2. 梳理 Companion 事件矩阵：触发条件、冷却、频率、是否主动。
3. 重写 `companion_conf.json` 文案，统一语气并减少重复。
4. 增加应用分类配置化能力和应用黑名单。
5. 补一轮手动验证清单，覆盖启动、摸头、返回、深夜、专注、工作场景。

如果你愿意，我下一步可以继续直接帮你做这 5 项里的第 1 和第 2 项，顺手给这个项目补一份“离线陪伴功能现状审计 + 开发任务拆解”文档。
