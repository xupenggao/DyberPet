# DyberPet 离线低打扰陪伴功能详细开发计划

## 1. 文档目标

这份文档用于指导 `DyberPet` 后续持续开发“单机、离线、低打扰陪伴功能”。

它不是产品概念稿，而是一份面向开发执行的计划文档，重点回答以下问题：

- 这条功能线的目标到底是什么
- 当前项目已经有哪些基础
- 接下来应该先做什么、后做什么
- 每个阶段具体改哪些模块
- 每个阶段完成后如何验收
- 哪些内容本轮暂时不做

---

## 2. 项目目标定义

### 2.1 总目标

在不依赖在线服务、不引入复杂 AI 架构的前提下，把当前桌宠升级为一个：

```text
长期驻留桌面、轻量感知场景、自然低频互动、不会频繁打扰用户的陪伴式桌宠
```

### 2.2 功能目标

这条开发线最终要达成的能力包括：

- 能根据时间、前台应用、专注状态、用户互动等轻量上下文做出低频陪伴反馈
- 能以气泡、轻动作、安静停留等方式表达存在感
- 能被用户明确控制频率、开关与静默行为
- 能长期稳定运行，不依赖网络
- 能形成统一、稳定的角色气质

### 2.3 非目标

本轮开发不追求：

- 通用聊天机器人能力
- 云端 LLM 对话能力
- 本地大模型部署
- 长期复杂记忆系统
- OCR、麦克风、摄像头等高隐私感知
- 向量数据库、知识库、检索增强

---

## 3. 产品原则

### 3.1 低打扰优先

所有设计都要服从一个原则：

```text
宁可少说，也不要乱说；
宁可安静陪着，也不要频繁冒泡。
```

### 3.2 陪伴感优先于智能感

用户更在意的是：

- 它是不是像在陪着我
- 它是不是会在合适的时候出现
- 它是不是有固定气质

而不是：

- 它是不是特别聪明
- 它是不是能回答很多问题

### 3.3 本地规则优先

优先使用：

- 时间规则
- 场景规则
- 事件触发
- 冷却机制
- 文案库

而不是引入复杂 AI 推理系统。

### 3.4 先稳定，再丰富

第一目标是把“少量能力做自然”，而不是把“很多能力堆进去”。

---

## 4. 当前项目基础盘点

从当前仓库情况看，已经具备以下可复用基础。

### 4.1 已有模块基础

#### `DyberPet/companion.py`

已有 `OfflineCompanion`，已经支持：

- 启动问候
- 摸头反馈
- 返回反馈
- 专注开始 / 结束反馈
- 工作场景陪伴
- 深夜陪伴
- 长时间静置陪伴
- 频率控制
- 每日触发上限
- 冷却控制

#### `DyberPet/active_window.py`

已有前台窗口跟踪能力，支持：

- Windows 前台窗口识别
- macOS 前台窗口识别
- 获取应用名 / 窗口范围

#### `DyberPet/bubbleManager.py`

已有成熟气泡系统，可复用：

- 气泡结构
- 文本显示
- icon 映射
- 触发显示

#### `DyberPet/settings.py`

已有设置项支持：

- 离线陪伴总开关
- 主动陪伴开关
- 场景陪伴开关
- 深夜陪伴开关
- 互动频率设置

#### `DyberPet/DyberSettings/BasicSettingUI.py`

已有设置界面入口，后续可以继续扩展控制项。

### 4.2 当前短板

当前虽然已经有原型，但仍有明显缺口：

- Companion 触发入口和规则还不够系统化
- 场景分类偏硬编码
- 文案风格尚未形成规范
- 与动作系统联动较弱
- 缺少完整的验收清单
- 缺少明确的阶段开发计划

---

## 5. 技术路线

### 5.1 总体思路

沿用当前 `PySide6` 桌宠架构，不推翻重建。

技术策略：

- 保留现有主程序结构
- 保留气泡系统
- 强化 `OfflineCompanion`
- 把陪伴功能做成明确的本地规则能力

### 5.2 推荐的模块职责划分

#### `DyberPet/companion.py`

职责：

- 陪伴规则判断
- 触发频率控制
- 上下文分类
- 消息选择
- 陪伴状态记录

#### `DyberPet/bubbleManager.py`

职责：

- 统一气泡输出协议
- 承接普通泡泡与陪伴泡泡

#### `DyberPet/active_window.py`

职责：

- 提供前台应用信息
- 不承担业务规则

#### `DyberPet/DyberPet.py`

职责：

- 接收用户事件
- 轮询状态
- 调用陪伴模块
- 转发到气泡或动作层

#### 配置文件

建议逐步形成以下结构：

- `res/icons/companion_conf.json`：文案库
- `res/icons/companion_rules.json`：规则参数，可后续新增
- `data/companion_state.json`：长期轻量状态，可后续新增

---

## 6. 版本开发策略

建议采用“稳定小步快跑”的方式，每个阶段都能独立交付。

开发阶段建议分为：

1. `V1` 原型收敛与正式化
2. `V2` 场景增强与低打扰完善
3. `V3` 人格一致性与动作联动
4. `V4` 长期陪伴与轻量成长

---

## 7. Phase 1：原型收敛与正式化

### 7.1 阶段目标

把当前已有的离线陪伴原型整理成一个稳定、可控、可验证的正式功能。

### 7.2 预期成果

- 陪伴触发路径清晰
- 设置项真实生效
- 触发频率合理
- 文案风格基础统一
- 能形成第一版可发布体验

### 7.3 具体任务

#### 任务 1：梳理 Companion 触发矩阵

输出一份触发表，至少包含：

- 事件名
- 触发来源
- 是否主动
- 触发条件
- 冷却时长
- 是否计入每日上限
- 是否允许在专注中触发
- 输出内容类型

建议覆盖：

- `companion_greeting`
- `companion_patpat`
- `companion_return`
- `companion_focus_start`
- `companion_focus_end`
- `companion_late_night`
- `companion_working`
- `companion_idle_presence`

#### 任务 2：核对所有入口接入状态

检查下列事件是否都实际接入：

- 程序启动
- 用户摸头
- 用户离开后返回
- 开始专注
- 结束专注
- 深夜轮询
- 工作场景轮询
- 长时间无交互轮询

输出“已接入 / 未接入 / 接入不稳定”的结果。

#### 任务 3：统一冷却与频率逻辑

需要验证并整理：

- 总间隔冷却
- 单类事件冷却
- 每日主动上限
- 频率倍率映射

目标：

- 不同频率档位有清晰差异
- 高频模式也不能变成骚扰模式

#### 任务 4：重整文案库第一版

对 `companion_conf.json` 做第一轮清理：

- 删掉语气不一致的句子
- 避免说教型表达
- 避免解释过多
- 避免重复句式

建议文案规范：

- 单句为主
- 4 到 18 字优先
- 轻陪伴、轻观察
- 不下命令

#### 任务 5：验证设置项

逐项验证：

- 总开关关闭后不再产生 Companion 输出
- 主动陪伴关闭后只保留被动反馈
- 场景陪伴关闭后不再基于前台应用触发
- 深夜陪伴关闭后夜间不再额外冒泡
- 频率切换能明显影响输出节奏

### 7.4 涉及模块

- `DyberPet/companion.py`
- `DyberPet/DyberPet.py`
- `DyberPet/settings.py`
- `DyberPet/DyberSettings/BasicSettingUI.py`
- `res/icons/companion_conf.json`

### 7.5 验收标准

- 一小时使用中没有明显打扰感
- 所有设置项生效符合预期
- 工作、深夜、摸头、返回、专注等场景可稳定触发
- 无联网依赖
- 不产生明显重复轰炸

---

## 8. Phase 2：场景增强与低打扰完善

### 8.1 阶段目标

让桌宠从“能在一些场景冒泡”提升为“更懂什么时候该安静、什么时候该出现”。

### 8.2 预期成果

- 场景分类更准确
- 打扰控制更强
- 用户可配置空间更大

### 8.3 具体任务

#### 任务 1：扩展应用分类体系

将当前应用分类从硬编码初版扩展为可维护结构。

建议支持的分类：

- ide
- design
- office
- browser
- media
- chat
- reading
- explorer
- general

注意：

- 不追求分类过细
- 先追求误判少

#### 任务 2：引入应用黑名单 / 白名单

建议新增配置能力：

- 黑名单应用不触发工作陪伴
- 白名单应用优先识别为特定场景

典型用途：

- 游戏不触发工作陪伴
- 聊天软件不误判为办公场景

#### 任务 3：新增静默时段

建议增加用户设置：

- 指定时段完全静默
- 深夜仅保留极低频陪伴

目标是进一步降低夜间和工作中打扰。

#### 任务 4：增加“忙碌中更克制”策略

例如：

- 前台应用持续变化时不主动冒泡
- 刚刚发生用户高频点击时不主动冒泡
- 专注期间只允许有限反馈

#### 任务 5：减少纯文本依赖

本阶段开始引入部分“非文本存在感”策略：

- 某些场景只切轻动作不冒泡
- 某些场景延后冒泡
- 某些场景直接放弃触发

### 8.4 涉及模块

- `DyberPet/companion.py`
- `DyberPet/active_window.py`
- `DyberPet/settings.py`
- `DyberPet/DyberSettings/BasicSettingUI.py`
- 新增可选配置文件

### 8.5 验收标准

- 场景误触发明显减少
- 深夜与工作时更安静
- 用户能明显感受到“它更会挑时机”
- 触发减少但陪伴感不下降

---

## 9. Phase 3：人格一致性与动作联动

### 9.1 阶段目标

建立稳定角色气质，让陪伴不只靠文本，而是靠整体表现。

### 9.2 预期成果

- 文案风格明显统一
- 陪伴反馈更像同一个角色
- 动作与文本之间形成轻联动

### 9.3 具体任务

#### 任务 1：制定陪伴文案规范

单独形成文案规则文档，建议包含：

- 角色定位
- 允许语气
- 禁止语气
- 句长建议
- 不同场景示例

建议角色基调：

- 温和
- 安静
- 少建议
- 少评判
- 少情绪戏剧化

#### 任务 2：整理文案标签体系

文案建议逐渐支持标签化，例如：

- `night_only`
- `focus_only`
- `app_category`
- `low_energy`
- `quiet_presence`

方便后续按状态选择。

#### 任务 3：引入轻量内部状态变量

只做极少量变量，不做复杂情绪系统。

建议尝试：

- `calmness`
- `sociability`
- `energy`

这些变量只影响：

- 主动触发概率
- 优先挑选的文案类型
- 是否偏安静动作

#### 任务 4：接入轻动作联动

例如：

- 摸头反馈时优先触发相关动作
- 深夜陪伴偏安静站立 / 休息动作
- 工作陪伴偏停留 / 观察动作

注意：

- 不重做动画系统
- 只做轻联动
- 联动失败不能影响主功能

### 9.4 涉及模块

- `DyberPet/companion.py`
- `DyberPet/DyberPet.py`
- 动画相关模块
- `res/icons/companion_conf.json`

### 9.5 验收标准

- 文案、时机、动作给人的感觉更一致
- 用户能感知到“这是同一个角色在陪伴我”
- 即使少说话，也能有存在感

---

## 10. Phase 4：长期陪伴与轻量成长

### 10.1 阶段目标

增强“陪伴持续发生过”的感觉，但仍坚持离线和低打扰。

### 10.2 预期成果

- 用户能感受到长期陪伴痕迹
- 桌宠会在低频节点表达“我们已经相处了一段时间”

### 10.3 具体任务

#### 任务 1：增加长期轻量统计

建议只记录少量数据：

- 陪伴天数
- 专注陪伴累计时长
- 摸头次数
- 工作场景陪伴触发次数
- 夜间陪伴触发次数

#### 任务 2：增加里程碑型文案

例如：

- 一起待了 7 天
- 最近常常见你在写代码
- 这段时间常常陪你到很晚

控制原则：

- 极低频
- 不要煽情过度
- 不要频繁提“陪伴时长”

#### 任务 3：增加轻量偏好设置

如后续需要，可增加：

- 喜欢被怎么称呼
- 夜间是否完全静默
- 哪些场景不要打扰

#### 任务 4：增加本地清理与重置能力

确保用户可以：

- 清除长期陪伴记录
- 重置 Companion 偏好
- 恢复默认策略

### 10.4 涉及模块

- `DyberPet/companion.py`
- `DyberPet/settings.py`
- `DyberPet/DyberSettings/BasicSettingUI.py`
- 新增轻量状态持久化文件

### 10.5 验收标准

- 长期信息存在感自然
- 不会让用户觉得“被记录得太多”
- 所有长期数据可关闭、可清理

---

## 11. 任务优先级

### P0：当前必须做

- Companion 触发矩阵整理
- 入口接入状态核对
- 设置项完整验证
- 文案库第一轮整理
- 冷却 / 上限 / 频率策略统一

### P1：短期紧接着做

- 应用分类扩展
- 黑名单 / 静默策略
- 更克制的忙碌场景策略
- 部分动作联动

### P2：中期可以做

- 文案规范文档
- 轻量状态变量
- 低频长期陪伴统计
- 里程碑文案

### P3：本轮不进入主线

- 云端聊天
- 本地 LLM
- 复杂记忆
- OCR / 麦克风 / 摄像头
- 向量存储

---

## 12. 每轮开发建议流程

为了让这条线能持续开发，建议每轮迭代固定采用以下流程。

### 第 1 步：先定义本轮只改什么

每轮最多聚焦一个小目标，例如：

- 只改深夜陪伴
- 只改应用分类
- 只改摸头反馈

避免一轮同时动太多逻辑。

### 第 2 步：补一份触发说明

每次改动前先明确：

- 新增什么触发
- 修改什么冷却
- 会影响哪些已有场景

### 第 3 步：改代码与配置

优先改：

- `companion.py`
- 配置 JSON
- 必要的设置 UI

尽量少扩散修改范围。

### 第 4 步：手动验证

至少验证：

- 正常情况是否触发
- 关闭设置后是否停用
- 是否出现重复触发
- 是否影响原有气泡系统

### 第 5 步：记录变更

建议每次改动都补一段开发记录：

- 改了什么
- 为什么改
- 有哪些已知限制

---

## 13. 推荐的验收清单

后续每轮开发都可以复用这份检查清单。

### 13.1 基础功能

- 程序启动时陪伴功能正常初始化
- Companion 总开关可生效
- Companion 关闭后不会再产生相关气泡

### 13.2 被动互动

- 摸头能触发陪伴反馈
- 长时间离开后返回能触发返回反馈
- 专注开始 / 结束反馈正常

### 13.3 主动互动

- 深夜场景低频触发
- 工作应用场景低频触发
- 长时间静置场景低频触发
- 不会在短时间内连续多次冒泡

### 13.4 低打扰验证

- 高频模式下也不会过于密集
- 专注模式下不会干扰
- 深夜不会频繁打扰
- 同类文案不会反复重复

### 13.5 兼容与稳定性

- Windows 下正常
- macOS 下基本正常
- 关闭网络时完全可用
- 不影响现有桌宠行为主流程

---

## 14. 风险与注意事项

### 14.1 最大风险不是“功能不够多”，而是“太吵”

这个功能最容易失败的地方不是技术实现，而是体验失衡。

要始终警惕：

- 触发太频繁
- 文案太像助手
- 语气太热情
- 场景判断错了还开口

### 14.2 不要过早设计大系统

当前最重要的是：

- 把已有离线陪伴原型做稳定
- 建立低打扰规则
- 形成可持续的小步迭代节奏

### 14.3 保持与现有桌宠逻辑兼容

不能因为陪伴功能影响：

- 动画线程稳定性
- 气泡系统稳定性
- 专注 / 番茄钟主流程
- 原有桌宠交互习惯

---

## 15. 建议的近期开发排期

如果按 4 个短迭代推进，建议如下。

### Iteration 1

目标：把当前 Companion 原型整理清楚

- 输出触发矩阵
- 输出现状审计表
- 验证设置项
- 梳理已接入 / 未接入点

### Iteration 2

目标：稳定第一版体验

- 调整冷却与每日上限
- 清理文案
- 统一频率档位策略
- 补手动验收脚本

### Iteration 3

目标：增强场景感与低打扰

- 扩展应用分类
- 加黑名单 / 静默时段
- 忙碌场景更克制

### Iteration 4

目标：增强角色感

- 统一文案规范
- 接轻动作联动
- 补少量长期陪伴表达

---

## 16. 本文档对应的下一步建议

为了真正进入持续开发状态，建议下一步直接落地以下产物：

1. `离线陪伴功能现状审计.md`
2. `Companion 触发矩阵.md`
3. `Companion 文案规范.md`
4. `Companion 手动验收清单.md`

如果这四份补齐，后续每一轮开发都会顺很多。

---

## 17. 最终结论

DyberPet 这条“离线低打扰陪伴”功能线，最适合的做法不是做成复杂 AI 系统，而是把现有桌宠能力向“更自然的陪伴体验”持续推进。

这条线的核心不是：

- 更聪明
- 更会聊天
- 更会分析用户

而是：

- 更会挑时机
- 更懂得克制
- 更像一直在身边

只要按这份计划稳步推进，DyberPet 完全有机会把“轻陪伴桌宠”这件事做得很有辨识度。
