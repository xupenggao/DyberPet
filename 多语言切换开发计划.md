# DyberPet 多语言切换开发计划

## 1. 文档目的

本文档基于《多语言切换测试用例》制定，用于指导 `DyberPet` 在 `i18n` 分支上完成“英文版本 + 应用内中英文切换”功能的完整开发工作。

文档目标不是描述愿景，而是明确：

- 当前代码基础是什么
- 这次开发究竟要交付什么
- 如何按阶段推进
- 每个阶段改哪些模块
- 每个阶段如何验证
- 哪些风险必须提前处理

---

## 2. 项目目标

### 2.1 总目标

在不破坏现有功能的前提下，为 `DyberPet` 提供完整、稳定、可维护的中英文双语支持，并支持用户在应用内切换语言。

### 2.2 本轮交付目标

- 支持 `简体中文` 与 `English`
- 语言选择可在设置页修改
- 切换结果写入配置并在重启后保持
- 核心窗口支持应用内即时刷新
- 核心业务文案按当前语言显示
- 缺失翻译时具备统一 fallback 机制
- 历史语言配置具备兼容回退能力

### 2.3 非目标

- 不在本轮引入第三语言
- 不做在线语言包下载
- 不做通用多语言平台或后台配置系统
- 不在本轮彻底重构所有资源格式，只覆盖用户可见关键路径

---

## 3. 当前现状分析

结合代码现状，项目已经具备半成品国际化基础，但还未形成完整能力。

### 3.1 已具备基础

#### 语言配置与加载链路

- `DyberPet/settings.py` 已有 `language_code`
- `DyberPet/settings.py` 已有 `lang_dict`
- `DyberPet/settings.py` 已有 `change_translator()`
- `run_DyberPet.py` 启动时已安装 `FluentTranslator` 和 `settings.translator`

#### 设置入口

- `DyberPet/DyberSettings/BasicSettingUI.py` 已存在语言下拉框
- 已存在 `_LanguageChanged()` 事件
- 已存在语言切换后的配置保存

#### 翻译资源基础

- `langs.pro` 已接入翻译源文件
- `res/language/` 已存在 `ts/qm/json` 基础资源

#### 部分数据型文案已支持多语言

- 金币名已使用 `{ zh_CN, en_US }` 结构
- 离线陪伴文案已按语言分组存放在 `companion_conf.json`

### 3.2 当前明确缺口

#### 运行时刷新能力不完整

- 当前切换语言后仍以“重启后生效”为主
- 多数页面没有成体系的 `retranslateUi()` 或刷新入口

#### 数据型文案结构不统一

- 部分 JSON 已是多语言结构
- 但物品描述、角色资料、标签、部分提示文案仍为单语言

#### 全局文本状态存在污染风险

- `settings.change_translator()` 中会直接修改 `TIER_NAMES/HUNGERSTR/FAVORSTR`
- 多次切换语言可能造成状态不可逆或混合语言

#### 存在编码遗留问题

- 当前仓库中可见部分源码和资源文件存在乱码表现
- 如果不先清理编码，后续翻译维护会持续恶化

---

## 4. 开发原则

### 4.1 先稳定，再扩范围

先完成核心链路稳定，再扩展次级页面，不做一次性全量铺开。

### 4.2 先统一机制，再补文案

先解决切换机制、刷新机制、fallback 机制，再投入翻译补全工作。

### 4.3 UI 文案与数据文案分开治理

- `self.tr(...)` 属于 Qt UI 文案
- JSON/配置读取属于数据文案

两者采用不同方案，但必须有统一语言来源。

### 4.4 小步验收

每完成一个阶段，就对照《多语言切换测试用例》执行对应验证，不等全部做完再集中排错。

---

## 5. 目标架构

### 5.1 统一语言状态入口

以 `DyberPet/settings.py` 作为唯一语言状态源，负责：

- 当前语言代码
- 受支持语言列表
- 语言校验与 fallback
- translator 生命周期管理

### 5.2 统一语言切换流程

建议语言切换采用以下标准流程：

1. 用户在设置页选择语言
2. 更新 `settings.language_code`
3. 持久化到 `settings.json`
4. 重新安装 Qt translator
5. 触发全局语言变更事件
6. 主窗口与主要子界面执行 `retranslateUi()` 或局部刷新
7. 数据型文案重新读取或刷新绑定

### 5.3 统一数据文案 fallback

建议统一使用以下结构：

```json
{
  "zh_CN": "中文文本",
  "en_US": "English text",
  "default": "中文文本"
}
```

读取时规则建议为：

1. 优先取当前 `language_code`
2. 没有则取 `default`
3. 还没有则取任一可用字符串
4. 若仍缺失，返回安全空串或源字段名

---

## 6. 分阶段开发计划

## 阶段一：国际化基线清理

### 目标

建立一个可继续开发的稳定基线，先解决编码、语言资源、配置回退等底层问题。

### 任务

- 清理明显乱码文件，统一为 UTF-8
- 检查并修正 `res/language/language.json`
- 核查 `langs.pro` 收录范围
- 梳理所有语言来源：
  - Qt UI 文案
  - 运行时通知文案
  - JSON 数据文案
- 固化支持语言集合：`zh_CN`、`en_US`
- 明确非法语言码和历史值 `CN` 的回退逻辑

### 涉及模块

- `DyberPet/settings.py`
- `res/language/language.json`
- `langs.pro`
- 乱码明显的源码或资源文件

### 产出

- 统一编码的语言基础资源
- 可预测的语言校验与 fallback 规则

### 验证用例

- `I18N-001`
- `I18N-002`
- `I18N-023`
- `I18N-024`
- `I18N-027`
- `I18N-028`
- `I18N-030`

---

## 阶段二：语言切换内核改造

### 目标

把现有“保存语言配置”升级为“可驱动应用内语言切换”的内核能力。

### 任务

- 重构 `settings.change_translator()`
- 避免直接污染全局翻译后的静态文本
- 补充 translator 安装/替换策略
- 定义语言切换信号广播方式
- 统一主应用和子窗口的监听入口

### 涉及模块

- `DyberPet/settings.py`
- `run_DyberPet.py`
- `DyberPet/DyberPet.py`
- 可能新增语言切换辅助方法或信号桥接代码

### 产出

- 稳定的语言切换核心链路
- 不依赖重启的刷新触发机制

### 验证用例

- `I18N-003`
- `I18N-004`
- `I18N-005`
- `I18N-006`
- `I18N-031`
- `I18N-032`

---

## 阶段三：设置窗口与 Dashboard 热切换

### 目标

先打通最核心、用户最容易感知的两大窗口：设置主窗口和 Dashboard。

### 任务

- 为设置主窗口增加 `retranslateUi()` 或同等刷新方法
- 为 Dashboard 主窗口增加 `retranslateUi()` 或同等刷新方法
- 为主要子界面增加刷新方法：
  - `BasicSettingUI`
  - `GameSaveUI`
  - `CharCardUI`
  - `ItemCardUI`
  - `PetCardUI`
  - `statusUI`
  - `inventoryUI`
  - `shopUI`
  - `taskUI`
  - `animationUI`
- 刷新窗口标题、导航名称、标签、按钮、占位符、tooltip

### 涉及模块

- `DyberPet/DyberSettings/DyberControlPanel.py`
- `DyberPet/DyberSettings/BasicSettingUI.py`
- `DyberPet/Dashboard/DashboardUI.py`
- 相关子页面文件

### 产出

- 核心窗口切语言后的即时可见刷新

### 验证用例

- `I18N-007`
- `I18N-008`
- `I18N-009`
- `I18N-010`
- `I18N-021`
- `I18N-022`
- `I18N-029`

---

## 阶段四：通知、菜单与运行时文本刷新

### 目标

补齐非主页面但用户高频感知的运行时文本，包括托盘、右键菜单、通知和气泡。

### 任务

- 检查主宠物右键菜单刷新链路
- 检查系统托盘菜单刷新链路
- 检查通知模块中的 `self.tr(...)` 文案是否在切换后按新语言生效
- 检查运行时动态创建的弹窗、InfoBar、MessageBox 文案
- 确保切换语言后新创建的运行时对象使用新语言

### 涉及模块

- `DyberPet/DyberPet.py`
- `DyberPet/custom_widgets.py`
- `DyberPet/Notification.py`
- `DyberPet/extra_windows.py`
- 其他菜单生成位置

### 产出

- 运行时文本与当前语言保持一致

### 验证用例

- `I18N-011`
- `I18N-012`
- `I18N-013`
- `I18N-014`
- `I18N-029`

---

## 阶段五：数据型文案国际化

### 目标

解决“UI 文案能切换，但 JSON 配置文案不能切换”的问题。

### 任务

- 盘点所有用户可见 JSON 字段
- 优先改造关键字段为多语言结构：
  - `items_config.json` 中 `name`
  - `items_config.json` 中 `description`
  - `items_config.json` 中 `buff.description`
  - `info.json` 中 `petName`
  - `info.json` 中 `intro`
  - `info.json` 中标签及作者信息涉及的可见文本
- 封装统一读取函数，避免每个界面自己判断 `language_code`
- 保留兼容旧结构的 fallback 逻辑

### 涉及模块

- `DyberPet/conf.py`
- `DyberPet/companion.py`
- `DyberPet/Dashboard/*`
- `DyberPet/DyberSettings/*`
- `res/items/*`
- `res/role/*`
- `res/pet/*`

### 产出

- 核心数据型文案多语言支持
- 统一 fallback 工具函数

### 验证用例

- `I18N-015`
- `I18N-016`
- `I18N-017`
- `I18N-018`
- `I18N-019`
- `I18N-020`
- `I18N-033`

---

## 阶段六：翻译资源补全与发布验收

### 目标

补齐英文翻译资源，完成发布前回归和验收。

### 任务

- 补齐 `.ts` 中关键英文翻译
- 重新生成 `.qm`
- 检查遗漏 key
- 修复英文长文案引发的布局问题
- 执行完整测试用例中的 P0 与主要 P1
- 记录缺陷并完成收口

### 涉及模块

- `res/language/langs.zh_CN.ts`
- 可能新增或更新其他翻译资源
- 所有被验收中发现问题的模块

### 产出

- 可发布的中英文双语版本
- 一份完整验收记录

### 验证用例

- 全量执行 `P0`
- 按时间选择主要 `P1`

---

## 7. 任务拆分建议

为避免改动过大，建议按以下任务粒度拆分：

### 7.1 任务拆分原则

- 每个子任务只解决一个明确问题
- 每个子任务都要有明确输入、输出和验证点
- 先做“基础能力”和“回退能力”，再做“页面热切换”，最后做“文案补齐”
- 同一阶段内优先改公共链路，再改各页面局部逻辑

### 7.2 可直接派工的任务卡

#### 任务 A：编码与语言基线修复

- A1：统一资源文件编码为 UTF-8
- A2：修正 `res/language/language.json` 的内容与结构
- A3：梳理并固定支持语言集合 `zh_CN` / `en_US`
- A4：整理历史值 `CN` 的兼容映射
- A5：补齐缺失语言时的 fallback 规则
- 产出：稳定的语言基线与回退策略
- 验证：`I18N-023`、`I18N-024`、`I18N-027`、`I18N-030`

#### 任务 B：语言切换核心链路

- B1：梳理 `settings.language_code` 的唯一来源
- B2：重构 `change_translator()` 的安装与卸载流程
- B3：明确语言切换后的全局通知方式
- B4：避免在切换时直接污染静态全局文案
- B5：补齐启动时、切换时、重启后的状态同步
- 产出：可重复切换、可持久化、可恢复的语言核心链路
- 验证：`I18N-003`、`I18N-004`、`I18N-005`、`I18N-006`、`I18N-031`、`I18N-032`

#### 任务 C：设置主窗口热切换

- C1：为设置主窗口增加统一刷新入口
- C2：刷新主标题、分组标题、下拉框、按钮、占位符
- C3：刷新设置页内的子页面文案
- C4：保证语言切换后界面不需要重启
- 产出：设置页可热切换
- 验证：`I18N-007`、`I18N-009`、`I18N-010`

#### 任务 D：Dashboard 热切换

- D1：为 Dashboard 主框架增加统一刷新入口
- D2：刷新导航、标题、标签、按钮和 tooltip
- D3：补齐各子页的局部刷新方法
- D4：验证切换后首次打开与已打开页面行为一致
- 产出：Dashboard 核心界面可热切换
- 验证：`I18N-008`、`I18N-021`、`I18N-022`、`I18N-029`

#### 任务 E：托盘、菜单、通知、气泡

- E1：梳理运行时文本创建点
- E2：补齐托盘菜单刷新链路
- E3：补齐右键菜单刷新链路
- E4：补齐通知、气泡、弹窗的语言来源
- E5：确保切换后新建对象使用当前语言
- 产出：运行时文本与当前语言一致
- 验证：`I18N-011`、`I18N-012`、`I18N-013`、`I18N-014`

#### 任务 F：数据型文案结构升级

- F1：盘点所有用户可见 JSON 文案字段
- F2：先改物品相关字段为多语言结构
- F3：再改角色、陪伴、宠物相关字段
- F4：封装统一读取函数，避免页面自行判断语言
- F5：保留旧结构读取能力，确保历史资源可用
- 产出：数据文案支持按语言读取
- 验证：`I18N-015`、`I18N-016`、`I18N-017`、`I18N-018`、`I18N-019`、`I18N-020`、`I18N-033`

#### 任务 G：翻译补齐与发布验收

- G1：补齐 `langs.zh_CN.ts` 中缺失英文资源
- G2：重新生成 `.qm`
- G3：清理遗留未翻译 key
- G4：检查英文长文本布局问题
- G5：执行 P0 和重点 P1 回归
- 产出：可发布版本与完整验收记录
- 验证：全量 `P0`，重点 `P1`

### 7.3 推荐拆成的交付批次

- 批次 1：A + B
- 批次 2：C
- 批次 3：D
- 批次 4：E
- 批次 5：F
- 批次 6：G

### 7.4 每个子任务的最小完成标准

- 只改本任务范围内的文件和逻辑
- 至少能复现一个明确的验证用例
- 产出后不破坏既有支持语言和历史配置
- 任务结束时可以说明“改了什么、验证了什么、还剩什么风险”

---

## 8. 风险与应对

### 风险 1：运行时刷新遗漏

表现：

- 部分窗口切了语言，部分窗口还是旧语言

应对：

- 每个主窗口明确一个刷新入口
- 统一登记所有可见文本来源

### 风险 2：全局翻译状态污染

表现：

- 多次切换语言后，部分文本不再回到原语言

应对：

- 不直接修改原始全局文案值
- 保留源文本常量或改为按需计算

### 风险 3：旧 JSON 结构兼容复杂

表现：

- 新结构生效，但旧资源读取失败

应对：

- 统一 fallback 工具函数
- 支持旧字符串与新字典结构共存

### 风险 4：英文文本变长导致布局问题

表现：

- 按钮截断、导航溢出、弹窗挤压

应对：

- 在阶段三和阶段六安排专门布局检查

### 风险 5：编码遗留持续扩散

表现：

- 翻译文件、源码、资源多处出现乱码

应对：

- 阶段一先处理编码问题，再开始翻译补全

---

## 9. 阶段验收门槛

### 阶段一完成标准

- 支持语言集合确定
- 关键语言资源不乱码
- 非法语言码回退正常

### 阶段二完成标准

- 切换语言后无需重启即可驱动刷新链路
- 重启后仍保持选择语言

### 阶段三完成标准

- 设置窗口和 Dashboard 核心文案可热切换

### 阶段四完成标准

- 托盘、菜单、通知、气泡按当前语言显示

### 阶段五完成标准

- 金币、关键物品、角色资料、陪伴文案实现多语言显示

### 阶段六完成标准

- P0 用例全部通过
- 主要 P1 缺陷收敛
- 可进入发布或合并评审

---

## 10. 建议执行顺序

建议按以下顺序推进，不建议跳步：

1. 编码与语言资源基线修复
2. 语言切换核心链路改造
3. 设置主窗口热切换
4. Dashboard 热切换
5. 托盘、通知、菜单刷新
6. 数据型文案国际化
7. 英文翻译补齐
8. 全量回归与验收

---

## 11. 文档关系

本计划文档与《多语言切换测试用例》的关系如下：

- 本文档定义开发范围、阶段和执行顺序
- 测试用例文档定义每阶段的验证标准
- 开发过程中每个阶段结束后，应回到测试文档执行对应用例

---

## 12. 最终验收标准

满足以下条件时，可认为本轮多语言切换功能完成：

- 用户可在设置页切换 `简体中文` 与 `English`
- 核心窗口可在应用内正确刷新语言
- 重启后语言保持不变
- 核心业务文案可按当前语言显示
- 缺失翻译有统一 fallback
- 主要页面无明显乱码、混语、布局破坏
- 《多语言切换测试用例》中所有 P0 用例通过
